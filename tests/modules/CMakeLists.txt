# Tests for engine modules

message(STATUS "Configuring Module Tests...")

# Future module tests can be added here
# if(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/window)
#     add_subdirectory(window)
# endif()
